with "xmlada";
project Default is
   for Main use ("validate.adb");
   package Binder is
      for Switches ("Ada") use ("-E", "-g");
   end Binder;
   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;
end Default;
