project Convert is
   for Main use ("convert.adb");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   package Compiler is
      for Switches ("Ada") use ("-gnata", "-gnatwa", "-gnatyaI");
   end Compiler;
end Convert;
